version: "3.8"
services:
  leafwriter:
    build:
      dockerfile: Dockerfile
    environment:
      - AUTH_API_URL=http://localhost:3001  
      - KEYCLOAK_URL=http://localhost:8090/auth 
      - NSSI_URL=http://localhost:8080 
           
    ports:
      - 3000:3000